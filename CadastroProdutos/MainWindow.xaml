<Window x:Class="CadastroProdutos.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:CadastroProdutos"
        mc:Ignorable="d"
        Title="Cadastro de Produtos" Height="450" Width="800">
    <Grid Margin = "10">
        <Grid.RowDefinitions>
            <RowDefinition Height = "Auto"/>
            <RowDefinition Height = "Auto"/>
            <RowDefinition Height = "Auto"/>
            <RowDefinition Height = "Auto"/>
            <RowDefinition Height = "*"/>
        </Grid.RowDefinitions>

        <!-- Formulario -->
        <StackPanel Orientation = "Horizontal" Margin = "0 0 0 10">
            <TextBox x:Name="txtName" Width = "150" Margin = "0 0 10 0"/> 
            <TextBox x:Name="txtCategory" Width = "150" Margin = "0 0 10 0"/>
            <TextBox x:Name="txtPrice" Width = "150" Margin = "0 0 10 0"/>
            <TextBox x:Name="txtQuantity" Width = "150" Margin = "0 0 10 0"/>
            <Button Content="Adicionar" Click="Adicionar_Click" Width = "100"></Button>
        </StackPanel>

        <!-- Data Grid -->
        <DataGrid x:Name="dgProducts" Grid.Row = "2" AutoGenerateColumns = "False" ItemsSource = "{Binding}">
            <DataGrid.Columns>
                <DataGridTextColumn Header="Nome" Binding = "{Binding Name}" Width = "*" />
                <DataGridTextColumn Header="Categoria" Binding = "{Binding Category}" Width = "*" />
                <DataGridTextColumn Header="Preço" Binding = "{Binding Price}" Width = "*" />
                <DataGridTextColumn Header="Quantidade" Binding = "{Binding Quantity}" Width = "*" />
            </DataGrid.Columns>
        </DataGrid>

        <!-- Botão de Remover -->
        <Button Grid.Row="1" Content="Remover Selecionado" Width="160" HorizontalAlignment="Left" Click="Remover_Click"></Button>
    </Grid>
</Window>